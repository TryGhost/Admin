/* eslint-env node */
/* eslint-disable no-var,object-shorthand */

'use strict';

module.exports = function (environment) {
    var ENV = {
        modulePrefix: 'slack',
        environment: environment
    };

    return ENV;
};
