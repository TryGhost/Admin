{{#if (or this.whatsNew.hasNewMajor this.whatsNew.isShowingModal)}}
    <GhFullscreenModal
        @modal="whats-new"
        @modifier="wide"
        @close={{fn this.whatsNew.closeModal}}
    />
{{/if}}