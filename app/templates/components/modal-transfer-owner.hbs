<header class="modal-header">
    <h1>{{t "Transfer Ownership"}}</h1>
</header>
<a class="close" href="" title="{{t "common.Close"}}" {{action "closeModal"}}>{{svg-jar "close"}}<span class="hidden">{{t "common.Close"}}</span></a>

<div class="modal-body">
    <p>
        {{t "Are you sure you want to transfer the ownership of this blog? You will not be able to undo this action."}}
    </p>
</div>

<div class="modal-footer">
    <button {{action "closeModal"}} class="gh-btn"><span>{{t "common.Cancel"}}</span></button>
    {{gh-task-button (t "taskButton.Yep - I'm sure") task=transferOwnership class="gh-btn gh-btn-red gh-btn-icon"}}
</div>
