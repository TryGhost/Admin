<header class="modal-header">
    <h1>{{t "Are you sure you want to delete this user?"}}</h1>
</header>
<a class="close" href="" title="{{t "Close"}}" {{action "closeModal"}}>{{svg-jar "close"}}<span class="hidden">{{t "Close"}}</span></a>

<div class="modal-body">
    <p>{{{t "<strong>WARNING:</strong> You are about to delete the user '<strong>{{name}}</strong>'. There is no way to recover this." name=user.name}}}</p>
    {{#if user.count.posts}}
        <ul>
            <li>{{t "The user will not have access to this blog anymore"}}</li>
            {{!-- TODO: proper pluralize and transplate 'post'  --}}
            <li>{{{t "<strong>{{posts}}</strong> created by this user will be deleted" posts=(pluralize user.count.posts 'post')}}}</li>
            <li>{{t "All other user data will be deleted"}}</li>
        </ul>
    {{else}}
        <ul>
            <li>{{t "User will not have access to this blog anymore"}}</li>
            <li>{{t "All user data will be deleted."}}</li>
        </ul>
    {{/if}}
</div>

<div class="modal-footer">
    <button {{action "closeModal"}} class="gh-btn"><span>{{t "Cancel"}}</span></button>
    {{#if user.count.posts}}
        {{gh-task-button (t "Delete user and their posts") successText=(t "Deleted") task=deleteUser class="gh-btn gh-btn-red gh-btn-icon"}}
    {{else}}
        {{gh-task-button (t "Delete user") successText=(t "Deleted") task=deleteUser class="gh-btn gh-btn-red gh-btn-icon"}}
    {{/if}}
</div>
