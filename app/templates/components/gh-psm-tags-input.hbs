<div class="form-group">
    <label for="tag-input">Tags</label>
    {{!-- TODO: handle tag removal so that new tags can be destroyed --}}
    {{gh-token-input
        selectedTokens=post.tags
        availableTokens=availableTags
        onChange=(action (mut post.tags))
        onCreate=(action "createTag")
        renderInPlace=true
    }}
    {{!-- {{gh-selectize
        id="tag-input"
        multiple=true
        selection=post.tags
        content=availableTags
        optionValuePath="content.uuid"
        optionLabelPath="content.name"
        openOnFocus=false
        create-item="addTag"
        remove-item="removeTag"
        plugins="remove_button, drag_drop"}} --}}
</div>
