<section class="gh-canvas">
    <GhCanvasHeader class="gh-canvas-header">
        <h2 class="gh-canvas-title" data-test-screen-title>
            <LinkTo @route="staff" data-test-staff-link={{true}}>{{t "Manual.Staff.Staff"}}</LinkTo>
            <span>{{svg-jar "arrow-right"}}</span>
            {{this.user.name}}
        </h2>

        <section class="view-actions">
            <div class="gh-btn gh-btn-primary"><span>{{t "Manual.Settings.Save"}}</span></div>
        </section>
    </GhCanvasHeader>

    <div class="gh-content">
        <GhLoadingSpinner />
    </div>
</section>
