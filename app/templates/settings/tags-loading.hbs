<section class="gh-view">
    <header class="view-header">
        {{#gh-view-title}}<span>{{t "Tags"}}</span>{{/gh-view-title}}
        <section class="view-actions">
            {{#link-to "settings.tags.new" class="gh-btn gh-btn-green"}}<span>{{t "New Tag"}}</span>{{/link-to}}
        </section>
    </header>

    <div class="gh-content">
        {{gh-loading-spinner}}
    </div>
</section>
